components:
  schemas: {}
  securitySchemes:
    jwt:
      bearerFormat: JWT
      scheme: bearer
      type: http
info:
  contact: {}
  description: LaTeX compilation and document processing
  title: LaTeX Worker API
  version: 1.0.0
openapi: 3.0.0
paths:
  /health:
    get:
      operationId: HealthController_check
      parameters: []
      responses:
        '200':
          description: ''
  /metrics:
    get:
      operationId: MetricsController_getMetrics
      parameters: []
      responses:
        '200':
          description: Prometheus metrics in text format
      summary: Get Prometheus metrics
      tags:
        - Metrics
servers: []
tags:
  - description: LaTeX compilation operations
    name: LaTeX
  - description: Metrics and monitoring
    name: Metrics
  - description: Health checks
    name: Health
